# Core dependencies (版本范围放宽，避免冲突)
openai>=1.40.0
anthropic>=0.28.0
tenacity>=8.2.0
typer>=0.12.3
rich>=13.7.1
pydantic>=2.7.4

# Web UI
streamlit==1.36.0
pyperclip==1.8.2

# Security (可选，用于安全存储API Key)
keyring==25.2.1
